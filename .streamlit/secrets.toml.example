# Optional. The app works without an API key using its transparent offline question generator
# and guided estimator. Add a key only if you want live LLM suggestions.
OPENAI_API_KEY = ""
OPENAI_MODEL = "gpt-5-mini"
